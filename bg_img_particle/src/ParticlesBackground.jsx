
import Particles from 'react-tsparticles'
import particleConfig from './config/particles_config'
const ParticlesBackground = () => {
    return (
        <Particles param={particleConfig}></Particles>
    )
}

export default ParticlesBackground